<p>User info</p>
<div>
  <%= attachment_image_tag @user, :image %>
  <p>ショップ名：<%= @user.shop_name %></p>
  <p>説明：<%= @user.caption %></p>
  <p>ユーザーネーム：<%= @usere.user.name %></p>
  <p>投稿日時：<%= @user.created_at.strftime('%Y/%m/%d') %></p>
</div>
